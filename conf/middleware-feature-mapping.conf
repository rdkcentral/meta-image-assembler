# Middleware Feature Mapping Configuration
# This file maps DISTRO_FEATURES to middleware packages
# Use this to conditionally include/exclude middleware components based on product features

# Nuance voice components - only include if enable_nuance in DISTRO_FEATURES
MIDDLEWARE_FEATURE_nuance = "enable_nuance"
MIDDLEWARE_FEATURE_nuance_PACKAGES = " \
    nuanceeve \
"

# List all available feature mappings
MIDDLEWARE_AVAILABLE_FEATURES = " \
    nuance \
"
